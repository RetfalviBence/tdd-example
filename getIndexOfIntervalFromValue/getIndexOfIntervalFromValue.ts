/* 
task: 
getIndexOfIntervalFromValue:
Function that get:
  intervals: Interval[],
  value: number,
Decide the value where is in the interval, the function should return with an index.

If the value is out of range, should return with 0 or the last index.
*/